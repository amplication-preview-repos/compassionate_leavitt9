apiVersion: v1
kind: Service
metadata:
  name: {{ include "setup-read-service.fullname" . }}
  labels:
    {{- include "setup-read-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "setup-read-service.selectorLabels" . | nindent 4 }}
